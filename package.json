{
  "name": "test-simple-react-admin",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cross-env": "^7.0.2",
    "firebase": "7.12.0",
    "react": "^16.13.0",
    "react-admin": "^3.x",
    "react-admin-firebase": "^3.1.3",
    "react-dom": "^16.x",
    "react-scripts": "3.4.0",
    "redux-saga": "^1.1.3"
  },
  "scripts": {
    "start": "cross-env react-scripts start --watch",
    "dev": "cross-env BROWSER=none yarn clean:lib && webpack --progress --colors --watch --env dev",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
